// Hex Bloom - the patch that ran inline on the original contact page, kept as-is.
src(o0).modulateScale(noise(0.4)).blend(osc(3, 0.5, 1.8).mask(shape(6, 0.46))).colorama(0.004).scale(0.85).out(o0)
speed = 0.5
